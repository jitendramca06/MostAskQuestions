package com.mostaskquestions.arrays;

/**
 * @author Jitendra Kumar : 24/8/18
 */
public class LeftRightShift {
    private static int leftShift(int number, int shift) {
        //n << m = n.2 pow(m)
        //n >> m = n / 2.pow(m)
        //
        return 0;
    }
}
